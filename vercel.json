{
  "builds": [
    {
      "src": "app/api/webhooks/stripe/route.ts",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/api/webhooks/stripe",
      "dest": "api/webhooks/stripe/route.ts"
    }
  ]
}